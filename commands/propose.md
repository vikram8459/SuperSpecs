---
description: "Draft an OpenSpec change proposal (proposal.md, spec deltas, design.md, tasks.md)"
---

Use the `superspecs:openspec-propose` skill to draft a new OpenSpec change folder under `openspec/changes/<change-id>/`. Do NOT write production code until the proposal is reviewed and approved. After approval, hand off to `superspecs:writing-plans`.
